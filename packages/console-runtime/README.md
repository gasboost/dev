# @gasboost/console-runtime

Localhost runtime for typed, registered developer-console operations.

The browser can invoke only registered operation IDs with input accepted by each operation's Zod schema. The runtime does not expose a shell or generic command endpoint.
