{
  "name": "root",
  "packageManager": "pnpm@10.18.0",
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "scripts": {
    "build": "pnpm -r build",
    "smoke:packed": "node scripts/smoke-packed-packages.mjs",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
